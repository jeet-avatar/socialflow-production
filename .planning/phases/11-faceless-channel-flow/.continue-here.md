---
phase: 11-faceless-channel-flow
task: 6
total_tasks: 14
status: in_progress
last_updated: 2026-04-17T05:30:00.000Z
---

<current_state>
Tasks 1-5 complete. Task 6 (trending endpoints) is next.
Repo: /tmp/sf-prod (jeet-avatar/socialflow-production, branch: main, HEAD: 3aa48c1)
All pushed to remote.
</current_state>

<completed_work>

- Task 1: feedparser added to backend/requirements.txt — commit 6c44b7a
- Task 2: notifications collection + 3 indexes in db_init.py — commit 9988c3e
- Task 3: trending_service.py (Google News RSS + Redis cache, 4 tests) — commit ac22648 + fix 0105a17
- Task 4: /content/voice-previews enriched to return {voices:[{voice_id,name,description,preview_url}]} — commit 777fed3
- Task 5: channel_routes.py — 4 new endpoints (list videos, approve, reject, accept-disclaimer) + ChannelUpdate.setup_complete field + _qv_col/_notif_col helpers — 9 tests passing — commit 3aa48c1

### Key finding from Task 5:
- Tests live in `backend/tests/` (where pytest.ini points), NOT `backend/app/tests/`. The conftest there has mongomock + full app + dev-bypass auth fixtures — use those.
- Backend bakes code at build time. Every code change requires: `docker-compose up -d --build backend && sleep 10`
</completed_work>

<remaining_work>

### Chunk 2 (continued)
- **Task 6**: Add trending-suggestions + channel trending + from-trend endpoints to channel_routes.py
  - CRITICAL: `GET /channels/trending-suggestions` (no auth, no channel_id) MUST be registered BEFORE any `/{channel_id}` routes — FastAPI matches in registration order. Add it at the TOP of the appended block.
  - `GET /channels/{channel_id}/trending` — returns niche-filtered topics
  - `POST /channels/{channel_id}/videos/from-trend` — writes queued_video to DB FIRST, then dispatches Celery render_video_task

### Chunk 3: Backend Scheduler + Notifications
- **Task 7**: Update scheduler.py `_run_channel_pipeline` to write queued_video record after render_video_task.delay() + schedule 80% Celery countdown
- **Task 8**: Create notification_tasks.py with send_review_reminder Celery task

### Chunk 4: Frontend Wizard
- **Task 9**: Create frontend/src/components/channels/ChannelWizard.tsx (3-step)
- **Task 10**: Wire ChannelWizard into ChannelDashboard.tsx (replace create modal)

### Chunk 5: Frontend Channel Home
- **Task 11**: Create VideoPreviewModal.tsx + AutoPostDisclaimer.tsx
- **Task 12**: Create ReviewQueue.tsx (polls every 30s)
- **Task 13**: Create TrendingFeed.tsx (sidebar + "Make video" button)
- **Task 14**: Create ChannelHome.tsx + wire into Dashboard.tsx (selectedChannel state + onOpenChannelHome prop)
</remaining_work>

<decisions_made>

- ChannelHome = panel in Dashboard (no React Router) — mirrors activePipelineChannel/showPipelineOverlay pattern
- Trending topics = Google News RSS via feedparser (free, no key) — Redis-cached 6h per niche
- Voice picker wizard Step 3 fetches /content/voice-previews; on create, calls POST /model-config/ as second request
- auto_post = "run cron schedule"; review gate is separate concern — creators always review unless auto_post_disclaimer_accepted=True
- Disclaimer acceptance stored on channel doc (one-time per channel)
- queued_videos collection (NOT videos collection) — already has channel_id+status index
- voice_id stored in model_configs collection via POST /model-config/
- Notification email: use _send() from utils/notifications.py (send_notification() does NOT exist)
- ObjectId required: db["channels"].find_one({"_id": ObjectId(channel_id)}) — always use bson.ObjectId
- GET /notifications/unread endpoint DEFERRED — no frontend consumer exists either

### Important env/infra findings this session:
- Backend Docker image bakes code at build time (no volume mounts) — MUST rebuild after every Python file change
- Root `backend/requirements.txt` is what the Dockerfile installs (NOT backend/app/requirements.txt)
- Tests go in `backend/tests/` (pytest.ini points there, conftest has mongomock/full-app fixtures)
- `backend/app/tests/` is a secondary location; real test runner ignores it
- `pytest` must NOT be in `backend/requirements.txt` (production image)
</decisions_made>

<blockers>

None. Local stack healthy: `{"status":"healthy","services":{"redis":"ok","mongodb":"ok","celery":"ok"}}`
</blockers>

<context>
SocialFlow faceless content automation: niche → AI script → ElevenLabs TTS → Remotion video → review queue → post.

Plan: docs/superpowers/plans/2026-04-16-faceless-channel-flow.md
Spec: docs/superpowers/specs/2026-04-16-faceless-channel-flow-design.md

Local dev:
  cd /tmp/sf-prod
  docker-compose up -d --build backend  (rebuild required after any Python change)
  cd frontend && npm run dev -- --port 5180

Auth bypass: Authorization: Bearer dev-bypass / VITE_DEV_BYPASS_AUTH=true
Backend: http://localhost:8000  Frontend: http://localhost:5180

Use subagent-driven-development skill to continue execution.
</context>

<next_action>
Run: /gsd:resume-work
Then invoke superpowers:subagent-driven-development to continue from Task 6.

Task 6 prompt context needed:
- trending-suggestions MUST be registered before /{channel_id} routes (FastAPI order matters)
- from-trend MUST write to _qv_col().insert_one() BEFORE dispatching render_video_task.delay()
- render_video_task is imported from worker.video_tasks
- TrendVideoRequest body model needs to be added (class TrendVideoRequest(BaseModel): topic: str)
</next_action>
